# DevSecOps Demo Project 🚀

Учебный проект для изучения DevSecOps практик. Показывает полный цикл разработки безопасного приложения.

## Что это такое?

Это простое веб-приложение (API), которое автоматически:
- ✅ Тестируется при каждом изменении кода
- 🔒 Проверяется на уязвимости безопасности
- 🐳 Упаковывается в Docker-контейнер
- ☸️ Разворачивается в Kubernetes
- 🚀 Деплоится автоматически

## Технологии

| Технология | Для чего |
|------------|----------|
| **Python + FastAPI** | Само приложение (REST API) |
| **pytest** | Автоматические тесты |
| **Docker** | Упаковка приложения в контейнер |
| **Trivy** | Сканер безопасности (ищет уязвимости) |
| **Kubernetes** | Оркестрация контейнеров |
| **Terraform** | Инфраструктура как код |
| **GitHub Actions** | CI/CD пайплайн (автоматизация) |

## Структура проекта

repos_3/
├── app/                    # Код приложения
│   └── main.py            # FastAPI приложение
├── tests/                  # Тесты
│   └── test_main.py       # Автоматические тесты
├── k8s/                    # Kubernetes манифесты
│   ├── deployment.yaml    # Описание деплоя
│   └── service.yaml       # Описание сервиса
├── terraform/              # Инфраструктура как код
│   └── main.tf            # Terraform конфигурация
├── .github/workflows/      # CI/CD пайплайн
│   └── ci-cd.yaml         # GitHub Actions
├── Dockerfile             # Сборка Docker-образа
├── requirements.txt       # Python зависимости
└── README.md              # Этот файл


Clean

## Как работает CI/CD Pipeline

При каждом пуше в main ветку автоматически запускается:

🧪 ТЕСТЫ
└── Запускаются pytest тесты

🔒 СКАНИРОВАНИЕ БЕЗОПАСНОСТИ

└── Trivy проверяет код на уязвимости

🐳 СБОРКА
└── Собирается Docker-образ
└── Образ сканируется на уязвимости

🚀 ДЕПЛОЙ
└── Приложение разворачивается


Clean

## Локальный запуск

### Вариант 1: Напрямую Python

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить приложение
cd app
uvicorn main:app --reload

# Открыть в браузере
# http://localhost:8000/docs
Вариант 2: Через Docker

Bash
# Собрать образ
docker build -t devsecops-demo:v1 .

# Запустить контейнер
docker run -d -p 8000:8000 devsecops-demo:v1

# Открыть в браузере
# http://localhost:8000/docs
Вариант 3: В Kubernetes

Bash
# Запустить minikube
minikube start

# Загрузить образ в minikube
minikube image load devsecops-demo:v1

# Применить манифесты
kubectl apply -f k8s/

# Получить URL
minikube service devsecops-demo-service --url
API Эндпоинты
Метод	URL	Описание
GET	/	Главная страница
GET	/health	Проверка здоровья
GET	/users	Список пользователей
POST	/users	Создать пользователя
GET	/users/{id}	Получить пользователя
GET	/docs	Swagger документация
Сканирование безопасности

Bash
# Сканировать Docker-образ
trivy image devsecops-demo:v1

# Сканировать только критические уязвимости
trivy image --severity HIGH,CRITICAL devsecops-demo:v1

# Сканировать код
trivy fs .
Полезные команды

Bash
# Docker
docker ps                    # Список контейнеров
docker logs my-api           # Логи контейнера
docker stop my-api           # Остановить

# Kubernetes  
kubectl get pods             # Список подов
kubectl get services         # Список сервисов
kubectl logs <pod-name>      # Логи пода
kubectl scale deployment devsecops-demo --replicas=3

# Terraform
terraform init               # Инициализация
terraform plan               # Показать план
terraform apply              # Применить
Что я изучил
✅ Как писать REST API на Python
✅ Как писать автоматические тесты
✅ Как упаковывать приложение в Docker
✅ Как искать уязвимости с Trivy
✅ Как разворачивать в Kubernetes
✅ Как описывать инфраструктуру кодом (Terraform)
✅ Как настроить CI/CD pipeline (GitHub Actions)
